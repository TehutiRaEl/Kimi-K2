fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0

# colony_sdk — the Colony Standard Layer factory, previously hand-copied
# byte-identical into this repo, NAR2, and 4DBRAIN. THEHIVE (the Queen) is
# the canonical owner now; import surface (`from colony_sdk import
# ColonyConfig, make_colony_router`) is unchanged.
sovereign-hive-colony-sdk @ git+https://github.com/TehutiRaEl/THEHIVE.git#subdirectory=colony_sdk
